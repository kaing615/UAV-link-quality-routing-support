# Inference server dependencies (lighter than full requirements.txt)
# torch is installed separately before this file (CPU wheel):
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cpu
torch-geometric==2.8.0
numpy==2.4.1
pandas==3.0.1
fastapi==0.115.0
uvicorn[standard]==0.34.0
pydantic>=2.0
